type Group @model {
  id: ID!
  name: String!
  devices: [Device] @connection(name: "GroupDevices")
}
type Device @model {
  id: ID!
  title: String!
  group: Group @connection(name: "GroupDevices")
}
